group-advanced.group=Advanced

productUrlParam.hint=URL query parameter that carries the product ID for URL-mode context \
  (e.g. "?pid=123"). Change only if your URL scheme uses a different parameter name.
