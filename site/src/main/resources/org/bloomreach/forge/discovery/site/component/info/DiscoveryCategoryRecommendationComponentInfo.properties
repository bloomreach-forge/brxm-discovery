group-advanced.group=Advanced

categoryUrlParam.hint=URL query parameter that carries the category ID for URL-mode context \
  (e.g. "?category=shoes"). Change only if your URL scheme uses a different parameter name.
